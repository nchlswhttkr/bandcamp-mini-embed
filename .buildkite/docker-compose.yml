version: "3"
services:
  ci:
    working_dir: /bandcamp-mini-embed
    volumes:
      - ..:/bandcamp-mini-embed
    image: node:22
    environment:
      - CI # Wrangler CLI will do interactive prompts outside of CI
